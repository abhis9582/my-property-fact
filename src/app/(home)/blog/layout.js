import Media from "./page";

export const metadata = {
    title: "Media",
    description: "Media",
};

export default function MediaLayout({ children }) {
    return (
        <>
            {children}
        </>
    )
}